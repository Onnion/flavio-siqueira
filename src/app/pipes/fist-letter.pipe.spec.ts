import { FistLetterPipe } from './fist-letter.pipe';

describe('FistLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new FistLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
